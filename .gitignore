# Build outputs
src-tauri/target/
node_modules/
dist/
build/
*.exe
*.msi
*.dmg
*.deb
*.AppImage

# Signing keys (NEVER commit)
.keys/
*.tar.gz
*.zip

# Updater manifest (generated by CI release, not committed)
latest.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local data (datos sensibles del usuario)
*.local
.env
.env.local

# Tauri
src-tauri/gen/
src-tauri/Cargo.lock
src-tauri/.cargo/

# Node
npm-debug.log
.pnpm-store/

# CodeGraph
.codegraph/
# Local AI runtime state
.atl/

# Generated quality/audit reports (run scripts to regenerate)
quality-report.json
quality-iterate-report.json
catalog-export.json
catalog-export-diag.json
catalog-export-remediated.json
extraction-audit-report.json
assignment-audit-report.json

# Re-baseline de ground-truth: derivados locales del corpus, no referencia
# versionada (la referencia es manifest.json + verdicts.json, casadas entre si).
ground-truth/manifest.candidate.json
ground-truth/rebaseline-packet.json
audit-app-report.json

# Agent session exports
qwen-code-export-*.json

# Ground-truth renders (regenerable; ~24MB of PNGs)
ground-truth/*.png
ground-truth/crops/

# FASE 2 scratch / debug scripts (table-parser rewrite; no commitear)
scripts/_dbg_*
scripts/_img_audit/
scripts/_splice*
scripts/_t1.js

# Session exports (nunca versionar)
session-export-*.md
remediation-ledger.json
human-review-report.json
# Vision scratch (contact sheets)
_vis/
_vis2/
_vis3/
_vis4/
